using Advantech.Motion;

namespace MachineClassLibrary.Machine.MotionDevices
{
    public enum HomeRst
    {
        HOME_RESET_DIS = (int)HomeReset.HOME_RESET_DIS,
        HOME_RESET_EN = (int)HomeReset.HOME_RESET_EN,
        NOT_SUPPORT = (int)HomeReset.NOT_SUPPORT
    }
}
